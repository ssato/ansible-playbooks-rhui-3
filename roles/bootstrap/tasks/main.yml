---
- include_tasks: init_ssh.yml
  when:
    - in('bastion', group_names)

- include_tasks: setup_ssh.yml
- include_tasks: setup_yum_repos.yml

- include_tasks: setup_yum_repo_server.yml
  when:
    - in('repo_server', group_names)

# vim:sw=2:ts=2:et:
